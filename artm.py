def wyraz_n(a1, n, r):
    wyraz = a1 + (n-1) * r
    return wyraz


def suma_wyraz(a1, an, n):
    suma = 0.5 * (a1 + an) * n
    return suma


def srdk_wyraz(x, z):
    y = (x + z) * 0.5
    return y